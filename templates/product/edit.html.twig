{% extends 'admin/layout.html.twig' %}
{% import '_macros.html.twig' as mac %}
{% block title %}Modifier {{ product.reference }}{% endblock %}
{% block breadcrumb %}<a href="{{ path('product_index') }}" style="color:var(--text-muted);text-decoration:none;">Produits</a> / Modifier{% endblock %}
{% block content %}
{{ mac.flash_messages() }}
<div class="page-header">
    <div><h1 class="page-title">Modifier {{ product.reference }}</h1></div>
    <a href="{{ path('product_show', {id: product.id}) }}" class="btn-apex btn-apex-outline"><i class="bi bi-arrow-left"></i> Retour</a>
</div>
{{ include('product/_form.html.twig') }}
{% endblock %}
