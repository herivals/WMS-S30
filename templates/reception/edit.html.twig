{% extends 'admin/layout.html.twig' %}
{% import '_macros.html.twig' as mac %}
{% block title %}Modifier {{ reception.reference }}{% endblock %}
{% block breadcrumb %}<a href="{{ path('reception_index') }}" style="color:var(--text-muted);text-decoration:none;">Réceptions</a> / Modifier{% endblock %}
{% block content %}
{{ mac.flash_messages() }}
<div class="page-header">
    <div><h1 class="page-title">Modifier {{ reception.reference }}</h1></div>
    <a href="{{ path('reception_show', {id: reception.id}) }}" class="btn-apex btn-apex-outline"><i class="bi bi-arrow-left"></i> Retour</a>
</div>
{{ include('reception/_form.html.twig') }}
{% endblock %}
