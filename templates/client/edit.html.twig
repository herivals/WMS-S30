{% extends 'admin/layout.html.twig' %}{% import '_macros.html.twig' as mac %}{% block title %}Modifier {{ client.nomDeposant }}{% endblock %}{% block breadcrumb %}<a href="{{ path('client_index') }}" style="color:var(--text-muted);text-decoration:none;">Clients</a> / Modifier{% endblock %}{% block content %}{{ mac.flash_messages() }}<div class="page-header"><div><h1 class="page-title">Modifier {{ client.nomDeposant }}</h1></div><a href="{{ path('client_show', {id: client.id}) }}" class="btn-apex btn-apex-outline"><i class="bi bi-arrow-left"></i> Retour</a></div>{{ include('client/_form.html.twig') }}{% endblock %}
